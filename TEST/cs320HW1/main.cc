#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main(int argc, char *argv[]) {
	
    int MStartPositions[5] = {1,1,1,1,1};
    int Fiance[4] = {0,0,0,0};
 
    w = ComputeInvPrefLists(WPrefLists)

	while 0 not in Fiance:
		for i in range(1, instanceSize):
			for j in range(1, instanceSize):
				int wife = MPrefLists[i][j]
				if (Fiance[wife] != i):											//if man i and wife are already engaged then do not execute
					int reject = propose(i, wife, w, Fiance, MStartPositions)
																				//int index = m[reject][wife]	
					if (i != reject && reject != 0):	
						j = MStartPositions[reject] - 1
						i = reject
																				//int second = propose(reject, woman+1, Fiance, MStartPositions)
					else if (reject == 0):
						j = instanceSize
						
					else:
				else:
					j = instanceSize

	string = "("
	for i in Fiance:
		string += i+1
		string += ", "
		string += Fiance[i]
		string += ")"
		galeShapely.append(string)
	
	cout << "None, they like to live in darkness. haha \n";
}
